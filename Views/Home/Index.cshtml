@{
    ViewData["Title"] = "Trang chủ";
}

<!-- Hero Section -->
<section class="hero-section bg-primary text-white py-5" style="background: linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.5)), url('https://images.unsplash.com/photo-1517248135467-4c7edcad34c4?ixlib=rb-4.0.3') center/cover;">
    <div class="container">
        <div class="row align-items-center min-vh-50">
            <div class="col-lg-6">
                <h1 class="display-4 fw-bold mb-4">Chào mừng đến với Restaurantly</h1>
                <p class="lead mb-4">Trải nghiệm hương vị truyền thống Việt Nam với không gian ấm cúng và dịch vụ tận tâm</p>
                <div class="d-flex gap-3">
                    <a href="@Url.Action("Index", "Menu")" class="btn btn-warning btn-lg">
                        <i class="fas fa-utensils me-2"></i>Xem thực đơn
                    </a>
                    <a href="@Url.Action("Index", "Booking")" class="btn btn-outline-light btn-lg">
                        <i class="fas fa-calendar-alt me-2"></i>Đặt bàn ngay
                    </a>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Features Section -->
<section class="py-5">
    <div class="container">
        <div class="row text-center">
            <div class="col-md-4 mb-4">
                <div class="card h-100 border-0 shadow-sm">
                    <div class="card-body p-4">
                        <div class="text-primary mb-3">
                            <i class="fas fa-utensils fa-3x"></i>
                        </div>
                        <h5 class="card-title">Món ăn ngon</h5>
                        <p class="card-text">Các món ăn truyền thống được chế biến từ nguyên liệu tươi ngon nhất</p>
                    </div>
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="card h-100 border-0 shadow-sm">
                    <div class="card-body p-4">
                        <div class="text-primary mb-3">
                            <i class="fas fa-clock fa-3x"></i>
                        </div>
                        <h5 class="card-title">Phục vụ nhanh</h5>
                        <p class="card-text">Đội ngũ nhân viên chuyên nghiệp, phục vụ nhanh chóng và chu đáo</p>
                    </div>
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="card h-100 border-0 shadow-sm">
                    <div class="card-body p-4">
                        <div class="text-primary mb-3">
                            <i class="fas fa-heart fa-3x"></i>
                        </div>
                        <h5 class="card-title">Không gian ấm cúng</h5>
                        <p class="card-text">Không gian nhà hàng được thiết kế ấm cúng, phù hợp cho mọi dịp</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Featured Menu Section -->
@if (ViewBag.FeaturedItems != null && ((List<RestaurantMVC.Models.MenuItem>)ViewBag.FeaturedItems).Any())
{
    <section class="py-5 bg-light">
        <div class="container">
            <div class="text-center mb-5">
                <h2 class="display-5 fw-bold">Món ăn nổi bật</h2>
                <p class="lead">Những món ăn được yêu thích nhất tại nhà hàng</p>
            </div>
            <div class="row">
                @foreach (var item in (List<RestaurantMVC.Models.MenuItem>)ViewBag.FeaturedItems)
                {
                    <div class="col-lg-4 col-md-6 mb-4">
                        <div class="card h-100 shadow-sm">
                            <img src="@(string.IsNullOrEmpty(item.ImageUrl) ? "https://via.placeholder.com/300x200?text=" + Uri.EscapeDataString(item.Name) : item.ImageUrl)" 
                                 class="card-img-top" alt="@item.Name" style="height: 200px; object-fit: cover;">
                            <div class="card-body d-flex flex-column">
                                <h5 class="card-title">@item.Name</h5>
                                <p class="card-text flex-grow-1">@item.Description</p>
                                <div class="d-flex justify-content-between align-items-center">
                                    <span class="h5 text-primary mb-0">@item.Price.ToString("N0") VNĐ</span>
                                    <span class="badge bg-secondary">@item.Category</span>
                                </div>
                            </div>
                        </div>
                    </div>
                }
            </div>
            <div class="text-center mt-4">
                <a href="@Url.Action("Index", "Menu")" class="btn btn-primary btn-lg">
                    <i class="fas fa-eye me-2"></i>Xem tất cả món ăn
                </a>
            </div>
        </div>
    </section>
}

<!-- Gallery Section -->
<section class="py-5">
    <div class="container">
        <div class="text-center mb-5">
            <h2 class="display-5 fw-bold">Không gian nhà hàng</h2>
            <p class="lead">Khám phá không gian ấm cúng và sang trọng của chúng tôi</p>
        </div>
        <div class="row g-3">
            <div class="col-lg-4 col-md-6">
                <div class="gallery-item">
                    <img src="https://images.unsplash.com/photo-1555396273-367ea4eb4db5?ixlib=rb-4.0.3&w=400" 
                         class="img-fluid rounded shadow" alt="Không gian nhà hàng" style="height: 250px; width: 100%; object-fit: cover;">
                </div>
            </div>
            <div class="col-lg-4 col-md-6">
                <div class="gallery-item">
                    <img src="https://images.unsplash.com/photo-1514933651103-005eec06c04b?ixlib=rb-4.0.3&w=400" 
                         class="img-fluid rounded shadow" alt="Bàn ăn" style="height: 250px; width: 100%; object-fit: cover;">
                </div>
            </div>
            <div class="col-lg-4 col-md-6">
                <div class="gallery-item">
                    <img src="https://images.unsplash.com/photo-1551218808-94e220e084d2?ixlib=rb-4.0.3&w=400" 
                         class="img-fluid rounded shadow" alt="Bếp nhà hàng" style="height: 250px; width: 100%; object-fit: cover;">
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Testimonials Section -->
<section class="py-5 bg-light">
    <div class="container">
        <div class="text-center mb-5">
            <h2 class="display-5 fw-bold">Khách hàng nói gì về chúng tôi</h2>
            <p class="lead">Những phản hồi tích cực từ khách hàng thân thiết</p>
        </div>
        <div class="row">
            <div class="col-lg-4 mb-4">
                <div class="card h-100 border-0 shadow-sm">
                    <div class="card-body text-center p-4">
                        <div class="mb-3">
                            <i class="fas fa-star text-warning"></i>
                            <i class="fas fa-star text-warning"></i>
                            <i class="fas fa-star text-warning"></i>
                            <i class="fas fa-star text-warning"></i>
                            <i class="fas fa-star text-warning"></i>
                        </div>
                        <p class="card-text">"Món ăn rất ngon, không gian ấm cúng và nhân viên phục vụ rất chu đáo. Tôi sẽ quay lại nhiều lần nữa!"</p>
                        <div class="mt-3">
                            <strong>Nguyễn Văn An</strong>
                            <small class="text-muted d-block">Khách hàng thân thiết</small>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-lg-4 mb-4">
                <div class="card h-100 border-0 shadow-sm">
                    <div class="card-body text-center p-4">
                        <div class="mb-3">
                            <i class="fas fa-star text-warning"></i>
                            <i class="fas fa-star text-warning"></i>
                            <i class="fas fa-star text-warning"></i>
                            <i class="fas fa-star text-warning"></i>
                            <i class="fas fa-star text-warning"></i>
                        </div>
                        <p class="card-text">"Đây là nhà hàng tuyệt vời nhất mà tôi từng đến. Chất lượng món ăn và dịch vụ đều xuất sắc!"</p>
                        <div class="mt-3">
                            <strong>Trần Thị Bình</strong>
                            <small class="text-muted d-block">Food blogger</small>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-lg-4 mb-4">
                <div class="card h-100 border-0 shadow-sm">
                    <div class="card-body text-center p-4">
                        <div class="mb-3">
                            <i class="fas fa-star text-warning"></i>
                            <i class="fas fa-star text-warning"></i>
                            <i class="fas fa-star text-warning"></i>
                            <i class="fas fa-star text-warning"></i>
                            <i class="fas fa-star text-warning"></i>
                        </div>
                        <p class="card-text">"Nhà hàng có không gian đẹp, thích hợp cho các buổi hẹn hò và gặp gỡ bạn bè. Rất recommend!"</p>
                        <div class="mt-3">
                            <strong>Lê Minh Cường</strong>
                            <small class="text-muted d-block">Doanh nhân</small>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Stats Section -->
<section class="py-5 bg-primary text-white">
    <div class="container">
        <div class="row text-center">
            <div class="col-lg-3 col-md-6 mb-4">
                <div class="stat-item">
                    <h2 class="display-4 fw-bold mb-2" data-count="1000">0</h2>
                    <p class="lead">Khách hàng hài lòng</p>
                </div>
            </div>
            <div class="col-lg-3 col-md-6 mb-4">
                <div class="stat-item">
                    <h2 class="display-4 fw-bold mb-2" data-count="50">0</h2>
                    <p class="lead">Món ăn đặc sắc</p>
                </div>
            </div>
            <div class="col-lg-3 col-md-6 mb-4">
                <div class="stat-item">
                    <h2 class="display-4 fw-bold mb-2" data-count="5">0</h2>
                    <p class="lead">Năm kinh nghiệm</p>
                </div>
            </div>
            <div class="col-lg-3 col-md-6 mb-4">
                <div class="stat-item">
                    <h2 class="display-4 fw-bold mb-2" data-count="24">0</h2>
                    <p class="lead">Giờ phục vụ/ngày</p>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- CTA Section -->
<section class="py-5 bg-dark text-white">
    <div class="container text-center">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <h2 class="display-5 fw-bold mb-4">Sẵn sàng thưởng thức?</h2>
                <p class="lead mb-4">Đặt bàn ngay hôm nay để trải nghiệm những món ăn tuyệt vời tại Restaurantly</p>
                <div class="d-flex gap-3 justify-content-center">
                    <a href="@Url.Action("Index", "Booking")" class="btn btn-warning btn-lg">
                        <i class="fas fa-calendar-alt me-2"></i>Đặt bàn ngay
                    </a>
                    <a href="@Url.Action("Index", "Menu")" class="btn btn-outline-light btn-lg">
                        <i class="fas fa-utensils me-2"></i>Xem thực đơn
                    </a>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
.min-vh-50 {
    min-height: 50vh;
}
.hero-section {
    position: relative;
}

/* Gallery hover effects */
.gallery-item {
    overflow: hidden;
    border-radius: 0.5rem;
    transition: transform 0.3s ease;
}

.gallery-item:hover {
    transform: translateY(-5px);
}

.gallery-item img {
    transition: transform 0.3s ease;
}

.gallery-item:hover img {
    transform: scale(1.05);
}

/* Card hover effects */
.card {
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 25px rgba(0,0,0,0.15) !important;
}

/* Animation for stats */
.stat-item h2 {
    color: #ffc107;
}

/* Fade in animation */
.fade-in {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.6s ease, transform 0.6s ease;
}

.fade-in.visible {
    opacity: 1;
    transform: translateY(0);
}

/* Button hover effects */
.btn {
    transition: all 0.3s ease;
}

.btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 5px 15px rgba(0,0,0,0.2);
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
    // Counter animation for stats
    function animateCounter(element) {
        const target = parseInt(element.getAttribute('data-count'));
        const duration = 2000;
        const step = target / (duration / 16);
        let current = 0;
        
        const timer = setInterval(() => {
            current += step;
            if (current >= target) {
                current = target;
                clearInterval(timer);
            }
            element.textContent = Math.floor(current);
        }, 16);
    }
    
    // Intersection Observer for animations
    const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.classList.add('visible');
                
                // Animate counters when stats section is visible
                if (entry.target.classList.contains('stat-item')) {
                    const counter = entry.target.querySelector('[data-count]');
                    if (counter && !counter.classList.contains('animated')) {
                        counter.classList.add('animated');
                        animateCounter(counter);
                    }
                }
            }
        });
    }, { threshold: 0.1 });
    
    // Add fade-in class to elements and observe them
    const elementsToAnimate = document.querySelectorAll('.card, .gallery-item, .stat-item');
    elementsToAnimate.forEach(el => {
        el.classList.add('fade-in');
        observer.observe(el);
    });
    
    // Smooth scrolling for anchor links
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
        anchor.addEventListener('click', function (e) {
            e.preventDefault();
            const target = document.querySelector(this.getAttribute('href'));
            if (target) {
                target.scrollIntoView({
                    behavior: 'smooth',
                    block: 'start'
                });
            }
        });
    });
});
</script>
