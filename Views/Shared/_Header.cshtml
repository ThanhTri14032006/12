<!-- Top Contact Bar -->
<div class="top-bar bg-dark text-white py-2 d-none d-lg-block">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-md-6">
                <div class="d-flex align-items-center">
                    <i class="fas fa-envelope me-2 text-warning"></i>
                    <span class="me-4">contact@example.com</span>
                    <i class="fas fa-phone me-2 text-warning"></i>
                    <span>+1 5589 55488 55</span>
                </div>
            </div>
            <div class="col-md-6 text-end">
                <span class="me-3">En</span>
                <span>/</span>
                <span class="ms-3">De</span>
            </div>
        </div>
    </div>
</div>

<!-- Main Navigation -->
<nav class="navbar navbar-expand-lg navbar-dark fixed-top" style="background: rgba(12, 11, 9, 0.85); backdrop-filter: blur(10px); transition: all 0.3s ease;">
    <div class="container">
        <a class="navbar-brand fw-bold fs-2" href="@Url.Action("Index", "Home")" style="color: #cda45e; font-family: 'Playfair Display', serif;">
            Restaurantly
        </a>
        <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav mx-auto">
                <li class="nav-item">
                    <a class="nav-link px-3 py-2 text-white fw-medium" href="@Url.Action("Index", "Home")" style="transition: color 0.3s ease;">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link px-3 py-2 text-white fw-medium" href="@Url.Action("About", "Home")" style="transition: color 0.3s ease;">About</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link px-3 py-2 text-white fw-medium" href="@Url.Action("Index", "Menu")" style="transition: color 0.3s ease;">Menu</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link px-3 py-2 text-white fw-medium" href="#" style="transition: color 0.3s ease;">Specials</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link px-3 py-2 text-white fw-medium" href="#" style="transition: color 0.3s ease;">Events</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link px-3 py-2 text-white fw-medium" href="#" style="transition: color 0.3s ease;">Chefs</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link px-3 py-2 text-white fw-medium" href="#" style="transition: color 0.3s ease;">Gallery</a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle px-3 py-2 text-white fw-medium" href="#" role="button" data-bs-toggle="dropdown" style="transition: color 0.3s ease;">
                        Dropdown
                    </a>
                    <ul class="dropdown-menu bg-dark border-0 shadow">
                        <li><a class="dropdown-item text-white" href="@Url.Action("Check", "Booking")">Kiểm tra đặt bàn</a></li>
                        <li><hr class="dropdown-divider border-secondary"></li>
                        @if (User.Identity.IsAuthenticated)
                        {
                            <li><a class="dropdown-item text-white" href="@Url.Action("Index", "Admin")">
                                <i class="fas fa-tachometer-alt me-2"></i>Dashboard
                            </a></li>
                            <li>
                                <form asp-controller="Account" asp-action="Logout" method="post" class="d-inline">
                                    <button type="submit" class="dropdown-item text-white">
                                        <i class="fas fa-sign-out-alt me-2"></i>Đăng xuất
                                    </button>
                                </form>
                            </li>
                        }
                        else
                        {
                            <li><a class="dropdown-item text-white" href="@Url.Action("Login", "Account")">
                                <i class="fas fa-sign-in-alt me-2"></i>Đăng nhập
                            </a></li>
                        }
                    </ul>
                </li>
                <li class="nav-item">
                    <a class="nav-link px-3 py-2 text-white fw-medium" href="@Url.Action("Contact", "Home")" style="transition: color 0.3s ease;">Contact</a>
                </li>
            </ul>
            <div class="navbar-nav">
                <a class="btn btn-outline-warning px-4 py-2 fw-bold text-uppercase" 
                   href="@Url.Action("Index", "Booking")" 
                   style="border: 2px solid #cda45e; color: #cda45e; transition: all 0.3s ease; letter-spacing: 1px;">
                    BOOK A TABLE
                </a>
            </div>
        </div>
    </div>
</nav>

<!-- Header Styles -->
<style>
    .top-bar {
        font-size: 14px;
    }
    
    .navbar {
        box-shadow: 0 2px 20px rgba(0, 0, 0, 0.3);
    }
    
    .navbar-brand {
        font-size: 2rem !important;
        font-weight: 700 !important;
    }
    
    .nav-link:hover {
        color: #cda45e !important;
        transform: translateY(-2px);
    }
    
    .btn-outline-warning:hover {
        background-color: #cda45e;
        border-color: #cda45e;
        color: #000 !important;
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(205, 164, 94, 0.4);
    }
    
    .dropdown-menu {
        border-radius: 8px;
        margin-top: 10px;
    }
    
    .dropdown-item:hover {
        background-color: #cda45e;
        color: #000 !important;
    }
    
    /* Responsive adjustments */
    @media (max-width: 991px) {
        .navbar-brand {
            font-size: 1.5rem !important;
        }
        
        .btn-outline-warning {
            margin-top: 10px;
            width: 100%;
        }
    }
</style>